<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#42-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to '42 IT Consulting', root_path, :class => 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="42-collapse">
      <div class="nav navbar-nav slogan">
        <h5>The answer to the ultimate question of Life, the Universe, and Everything. <br /> <i><strong>Who do I go to for IT help?</strong></i></h5>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "Services", services_path, :class => 'btn btn-42-frame' %>
        </li>
        <% if user_signed_in? %>
          <li><%= link_to 'Sign Out', destroy_user_session_path, :class => 'btn btn-42-frame', :method => :delete %></li>
        <% else %>
          <li><%= link_to 'Log In', new_user_session_path, :class => 'btn btn-42-frame' %></li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to 'Request Assistance', new_request_path, :class => 'btn btn-42-frame' %></li>
        <% else %>
          <li><%= link_to 'Request Assistance', new_user_session_path, :class => 'btn btn-42-frame', :data => { :confirm => "Must be signed in to make a request."} %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
